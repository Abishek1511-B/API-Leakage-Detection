version: '3.8'
services:
  streamlit:
    build: .
    ports:
      - "8501:8501"
  api:
    build: .
    command: ["uvicorn", "api.leakrd_api:app", "--host", "0.0.0.0", "--port", "8000"]
    ports:
      - "8000:8000"
